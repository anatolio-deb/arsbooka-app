version: "3.9"

services:
  app:
    build: .
    environment:
      - VUE_APP_API_URL=http://localhost:8000/api/
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/code
    command: npm run serve
